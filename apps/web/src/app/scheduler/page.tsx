export default function SchedulerPage() {
  return (
    <div className="panel-right" style={{ overflow: 'auto' }}>
      <div className="page-title">Task Scheduler</div>
      <div className="page-subtitle">Phase 4 — coming soon.</div>
      <div className="card"><p style={{ color: 'var(--text-muted)', fontSize: 13 }}>Cron/webhook triggers will be wired in Phase 4 alongside Redis.</p></div>
    </div>
  );
}
